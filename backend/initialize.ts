export const initalizeNewGame = async (collectionName: string) => {
  console.log("Initializing new game" + collectionName);

  /**
   * const collectionOpensea = await getCollectionOpensea(collectionName)
   * const collectionStatsOpensea = await getCollectionStatsOpensea(collectionName)
   * const collectionStatsDappRadar = await getCollectionDappRadar(collectionName)
   * const collectionSentiment = await getCollectionSentiment(collectionName)
   * const collectionListings = await getListingsByCollectionsMetadata(collectionName)
   *
   * insert into the database with the vplay schema
   */
};
