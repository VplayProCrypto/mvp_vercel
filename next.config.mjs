/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "www.cryptokitties.co",
      "i.seadn.io",
      "places.decentraland.org",
      "hang.mypinata.cloud",
    ],
  },
};

export default nextConfig;
